services:

  mysql:
    image: mysql:8
    deploy:
      replicas: 1

  redis:
    image: redis:7
    deploy:
      replicas: 1

  kafka:
    image: apache/kafka:3.8
    deploy:
      replicas: 3

  member:
    image: user:1.0
    deploy:
      replicas: 3